# This file is read by python-dotenv (see backend/config.py)
# and keeps runtime configuration outside of the code
# You can change values here without modifying Python/JS files

# Backend (mock) settings
PORT=8000

# When the real API is ready, set this to that URL and
# optionally let the frontend call it directly.
API_BASE_URL=http://127.0.0.1:8000